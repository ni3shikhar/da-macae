mcp>=1.1.0
uvicorn>=0.34.0
aioodbc>=0.5.0
asyncpg>=0.30.0
azure-storage-blob>=12.24.0
azure-identity>=1.19.0
aiohttp>=3.9.0

# MySQL / MariaDB
aiomysql>=0.2.0

# Oracle Database (thin mode — no instant client needed)
oracledb>=2.0.0

# MongoDB
motor>=3.3.0

# Azure Cosmos DB (NoSQL API)
azure-cosmos>=4.7.0

# Snowflake
snowflake-connector-python>=3.6.0

# Azure Data Lake Storage Gen2
azure-storage-file-datalake>=12.14.0

# Databricks SQL
databricks-sql-connector>=3.0.0

# Google BigQuery
google-cloud-bigquery>=3.20.0

# File-based sources (CSV, Parquet, JSON)
pandas>=2.2.0
pyarrow>=15.0.0

# Azure Data Factory
azure-mgmt-datafactory>=5.0.0

# Azure Resource Management (resource groups, deployments)
# Pinned <25 because v25 removed deployments from ResourceManagementClient
azure-mgmt-resource>=23.0.0,<25.0.0

# Azure Storage Management (create/manage storage accounts)
azure-mgmt-storage>=21.0.0

# Azure SQL Management (create/manage SQL servers & databases)
azure-mgmt-sql>=3.0.0

# Azure Key Vault Management (create/manage key vaults)
azure-mgmt-keyvault>=10.0.0

# Azure Policy (compliance checks)
azure-mgmt-policyinsights>=1.0.0

# Synapse Analytics (artifacts client for linked-service management)
azure-synapse-artifacts>=0.18.0

# ── Security Assessment Tools ────────────────────────────────────────
# Network Security (NSGs, firewalls, virtual networks)
azure-mgmt-network>=25.0.0

# Compute Management (VMs, VM scale sets)
azure-mgmt-compute>=30.0.0

# Azure Security Center / Defender for Cloud
azure-mgmt-security>=6.0.0

# Role-Based Access Control (RBAC)
azure-mgmt-authorization>=4.0.0

# Backup and Recovery Services
azure-mgmt-recoveryservices>=2.0.0
azure-mgmt-recoveryservicesbackup>=7.0.0

# Web Apps / App Service
azure-mgmt-web>=7.0.0

# Azure Kubernetes Service (AKS)
azure-mgmt-containerservice>=30.0.0

# Subscription management
azure-mgmt-subscription>=3.0.0

# Azure Monitor (logging, diagnostics)
azure-mgmt-monitor>=6.0.0

# Log Analytics Workspace
azure-mgmt-loganalytics>=13.0.0

# Excel report generation
openpyxl>=3.1.0
